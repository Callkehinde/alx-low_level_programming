#include "main.h"

/**
 * _strlen - Returns the length of a string 
 * @str: string
 *
 * Return: length
 * */
size_t _strlen(const char  *str)
{
	size_t len = o;

	while (*str++)
		len++;

	return (len);
}
